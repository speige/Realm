namespace Realm.Ecs.Components.Core;

/// <summary>
///     Stores the calculated horizontal collision radius of an obstacle for navigation mesh baking.
/// </summary>
internal record struct CollisionRadius(float Value);
