namespace Realm.Ecs.Components.Core;

/// <summary>
///     A component for holding unit item slot contents (e.g. healing potions).
/// </summary>
internal record struct Inventory(int Potions);
