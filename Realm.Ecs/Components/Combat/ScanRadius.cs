namespace Realm.Ecs.Components.Combat;

/// <summary>
///     Defines the range in world units within which a unit scans for and acquires attack targets.
/// </summary>
internal record struct ScanRadius(float Value);
